<h3>Comments</h3>
<% puts "VIEW CHECK 2: comments = #{comments.inspect}" %>
<% comments.each do |comment| %>
  <div class="media">
    <div class="media-body">
      <h4 class="media-heading">
        <%= comment.body %>
      </h4>
    </div>
  </div>
<% end %>
